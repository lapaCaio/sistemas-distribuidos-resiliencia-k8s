apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-postgres
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: postgres
  duration: "60s"
